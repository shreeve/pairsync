<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1024 1024">
  <defs>
    <!-- Background gradient - dark blue/slate -->
    <linearGradient id="bgGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#2d3748"/>
      <stop offset="100%" style="stop-color:#1a202c"/>
    </linearGradient>

    <!-- Cyan gradient for arrows -->
    <linearGradient id="cyanGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#22d3ee"/>
      <stop offset="100%" style="stop-color:#06b6d4"/>
    </linearGradient>
  </defs>

  <!-- Background rounded square -->
  <rect x="40" y="40" width="944" height="944" rx="210" ry="210" fill="url(#bgGrad)"/>

  <!-- Subtle top highlight -->
  <rect x="60" y="50" width="904" height="100" rx="190" ry="50" fill="rgba(255,255,255,0.06)"/>

  <!-- Two-arrow sync symbol centered -->
  <g transform="translate(512, 512)">
    <!-- Top arrow (pointing right, curving down) -->
    <g transform="rotate(0)">
      <!-- Arc -->
      <path d="M -30 -200
               A 200 200 0 0 1 200 30"
            fill="none"
            stroke="url(#cyanGrad)"
            stroke-width="56"
            stroke-linecap="round"/>
      <!-- Arrowhead -->
      <polygon points="200,80 260,0 140,0" fill="url(#cyanGrad)"/>
    </g>

    <!-- Bottom arrow (pointing left, curving up) -->
    <g transform="rotate(180)">
      <!-- Arc -->
      <path d="M -30 -200
               A 200 200 0 0 1 200 30"
            fill="none"
            stroke="url(#cyanGrad)"
            stroke-width="56"
            stroke-linecap="round"/>
      <!-- Arrowhead -->
      <polygon points="200,80 260,0 140,0" fill="url(#cyanGrad)"/>
    </g>
  </g>
</svg>
